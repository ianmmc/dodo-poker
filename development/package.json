{
  "name": "dodo-poker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify-math": "python3 tools/verify-math.py",
    "setup-hooks": "ln -sf \"$(pwd)/.hooks/pre-commit\" \"$(pwd)/../.git/hooks/pre-commit\" && chmod +x .hooks/pre-commit && echo 'Pre-commit hook installed.'"
  },
  "dependencies": {
    "pokersolver": "^2.1.3"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.0"
  }
}
